# SQLite Setup

## System Requirements (FINISH)

- Stores App Config
  - Default time for each mode ('pomodoro', 'short break', 'long break'). All using int
  - auto start pomodoro (boolean)
  - auto start break (boolean)
  - long break interval (int)
- session history
  - session count for each day, count from pomodoro mode if finished.
- Tasks
  - Create new tasks, this will have it own timer setting: the timer default, how many session, interval long break, status (complete or not), repeated or only one do than delete

## Refactor Java

Refactor java code agar menjadi reposiotry pattern dan tasks objects.

- Migrasi aplikasi untuk menggunakan SQLite
- File do i need to put the database file?
- Make sure ready for deployment using jpackage

Jelaskan implementasi dari awal sampai siap untuk digunakan!
